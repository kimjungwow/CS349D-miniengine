# Milestone 1 Report (LaTeX)

## Files
- `main.tex`: report source
- `Makefile`: build helper

## Build
From repo root:

```bash
cd latex
make
```

Output PDF:
- `latex/report.pdf`

## Notes
- Fill all `\textit{fill}` placeholders in `main.tex` with your measured benchmark values and hardware details.
- The report already includes sections for:
  - design/implementation choices,
  - baseline vs optimized performance evidence,
  - additional code changes rationale,
  - hardware choice discussion.
